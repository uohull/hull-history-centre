<h3> Description </h3>

<% show_fields = [['biog_hist_ssm', 'Admin History'],
                  ['description_ssim', 'Description']] %>

<dl class="dl-horizontal  dl-invert">
  <% show_fields.each do |(solr_doc_key, label)| %>
    <%= show_attribute(document, solr_doc_key, label) %>
  <% end %>
</dl>
